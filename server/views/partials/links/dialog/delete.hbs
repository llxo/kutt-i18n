<div class="content">
  <h2>{{i18n 'dialog.delete.title'}}</h2>
  <p>
    {{i18n 'dialog.delete.confirmMessage' link=link}}
  </p>
  <div class="buttons">
    <button type="button" hx-on:click="closeDialog()">{{i18n 'dialog.cancel'}}</button>
    <button 
      type="button"
      class="danger confirm" 
      hx-delete="/api/links/{id}" 
      hx-ext="path-params" 
      hx-vals='{"id":"{{id}}"}' 
      hx-target="closest .content" 
      hx-swap="none" 
      hx-indicator="closest .content"
      hx-select-oob="#dialog-error"
    >
      <span>{{> icons/trash}}</span>
      {{i18n 'dialog.delete.button'}}
    </button>
    {{> icons/spinner}}
  </div>
  <div id="dialog-error">
    {{#if error}}
      <p class="error">{{error}}</p>
    {{/if}}
  </div>
</div>